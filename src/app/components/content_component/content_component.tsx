import styles from './content_component.module.css';

export default function ContentComponent() {
    return (
        <section className={ styles.container }>
            <div className={ styles.temp }>
            </div>
        </section>
    )
}